{
  "version": 2,
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "salus-frontend/build",
  "routes": [
    { "src": "/api/(.*)", "dest": "/server.js" },
    { "handle": "filesystem" },
    { "src": "/[^.]+", "dest": "/index.html" }
  ]
} 