import React from 'react';
import patternSvg from '../assets/images/pattern.svg';

// Componente che esporta l'URL del pattern
const BackgroundPattern = () => {
  return null; // Non renderizza nulla
};

// Esporta l'URL del pattern SVG
export const patternUrl = patternSvg;

export default BackgroundPattern; 